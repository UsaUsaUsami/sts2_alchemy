# 設計判断記録

## 2026-09-21: ビルドスクリプトの文字エンコーディング修正

- `scripts/build.ps1`・`scripts/install.ps1`はUTF-8（BOMなし）で保存されていた。Windows PowerShell 5.1はBOMなしUTF-8スクリプトをシステム既定コードページで読み込むため、日本語コメント・文字列を含む行でパースエラーが発生し、スクリプトが実行不能だった。
- 対応：両ファイルにUTF-8 BOMを付与。内容（コマンド・ロジック）は変更していない。
- 影響：スクリプトの実行結果には影響しない。今後このリポジトリに追加する`.ps1`ファイルも同様にBOM付きUTF-8で保存すること。

## 2026-09-21: 実機BaseLibをv3.2.0からv3.4.5へ更新

- 経緯：実機の`mods/BaseLib`がv3.2.0のままで、プロジェクトのビルド参照（`.tools/BaseLib-3.4.5`）・`Alchemist.json`の依存宣言（`min_version: 3.4.5`）と不一致だった。過去のゲームログでは、そもそもBaseLib自体が「設定で無効化」されており、Alchemistも一度も`mods`フォルダに導入されていなかった（＝実機起動確認は未実施だった）。
- 選択肢の検討：(a) 実機のBaseLibをv3.4.5へ更新、(b) GitHub最新のv3.4.7へ更新してから追従、(c) リポジトリ内の不整合のみ修正し実機変更はユーザーに委ねる。
- ユーザーの選択：(a)。理由：3.4.5はビルド時点で動作確認済みの参照バージョンであり、最新3.4.7への追従はAPI差分の確認負荷が増える。
- 実施内容：
  1. 実機`mods/BaseLib`の旧v3.2.0一式をプロジェクト外のスクラッチ領域にバックアップ。
  2. `.tools/BaseLib-3.4.5`の内容（BaseLib.dll/.json/.pck）で実機`mods/BaseLib`を上書き。
  3. `scripts/install.ps1`のバージョンチェックを`v3.2.0`→`v3.4.5`に修正。
  4. `scripts/install.ps1`を実行し、`dist/Alchemist`を実機`mods/Alchemist`へ導入。
  5. `%APPDATA%\SlayTheSpire2\steam\<SteamID>\settings.save`の`mod_settings.mod_list`に`BaseLib`（既存エントリを`is_enabled: true`に変更）と`Alchemist`（新規追加、`is_enabled: true`）を設定。他の導入済みMOD（RouteSuggest, Watcher, UnifiedSavePath）は元々`is_enabled: false`だったため変更していない。編集前の`settings.save`はスクラッチ領域にバックアップ済み。
- 影響：他の導入済みMOD3つはBaseLib依存だが、いずれも現状無効化設定のため直接の動作には影響しない。ただし将来それらを有効化する場合、v3.4.5との互換性は別途確認が必要（未検証）。
- 未決：v3.4.6/v3.4.7への追従、および旧v3.2.0バックアップの最終的な削除タイミング（当面保持）。

## 素材相・工房まわりの既存仮定（AGENTS.mdより維持）

このファイルは新規判断のみを追記していく。既存の採用方針・試作既定値・未決事項は`AGENTS.md`の「2. 仕様の扱い」を正とする。実装済みコード（`AlchemyState.cs`, `MaterialBox.cs`, `Cards.cs`, `WorkshopUi.cs`）はAGENTS.mdの採用方針・M1範囲に沿っている（工房は開発用の戦闘後入場、レシピは固定5種、軸カード《携帯錬金炉》は戦闘全体2回上限）。

## 2026-09-21: v0.2.0 工房の主役カード

- ユーザーの希望「強い切り札を作り、デッキの主役にしたい」に合わせ、通常素材2個のレシピを5種から12種へ拡張。順不同の全10組み合わせを網羅し、鉄2個・薬草2個には基礎カードと主役カードの選択を用意した。
- 7種の追加カードの数値・役割は `src/Alchemist.Core/ForgeCatalog.cs` に集約。大剣、弱体先行攻撃、廃棄連動パワー、毎ターン毒パワー、大量毒、全体攻撃を用意。既存5種も強化した。全て素材2個で、採取量・容量・通常報酬は変更しない。数値は強めの試作値であり、通しプレイで調整済みとはしない。
- 工房に役割・使い方・不足素材・作成可能なものへの絞り込みを追加。完成プレビューは消費なし。
- 新カードは単一のForgedCardモデルと安定したformula IDで保存する。既存カードのモデルID、ランスキーマ1を維持。既存カードの基礎値は更新後の定義に従う。新カードを含むセーブの旧MODへの降格は非対応。未知formulaは黙って初期化せず拒否する。
- 現段階の12種はM1の固定レシピ拡張。ベース3種×10組の基本文法、希少加工、専用工房ノードは未実装のまま。M3完了とは扱わない。
- 更新用scripts/update.ps1はゲーム終了を確認し、旧MODのDLLとマニフェストをworkspaceのartifacts/backupsに保存して更新する。コピー後ハッシュを照合し、失敗時は旧版へ戻す。

## 2026-09-21: v0.2.1 工房UI

- 工房を一列のテキスト一覧から、画面サイズに追従する二列レイアウトへ変更。左に素材容量・4素材の所持数・素材の役割・状態メッセージ・退出操作、右にスクロール可能なレシピ一覧を置く。
- 各レシピをカード状に分離し、名前、役割、必要素材、デッキでの使い方、現在効果、強化後効果、錬成操作を一まとまりにした。作成可能な候補は金色の枠、素材不足は暗い表示と不足数で区別する。
- 工房へ入った時は作成可能なレシピだけを初期表示し、全レシピとの切り替えを用意。素材受け取り・満杯時の交換画面も同じ枠組みに統合した。
- 既存の錬成処理、保存形式、レシピ内容、素材経済は変更しない。

## 2026-09-21: v0.2.2 完成カード選択UI

- 工房のレシピを説明パネルで選ぶ方式から、ゲーム標準のカード表示を使って完成後のカード12枚を並べる方式へ変更した。
- 一覧でカードを選ぶと、一枚を大きく表示する確認画面へ移る。確認画面で必要素材、役割、運用案、通常効果、強化後効果を確認し、「このカードを作る」または「一覧へ戻る」を選ぶ。
- 工房へ入った直後は全カードを表示する。作成可能なカードだけへの絞り込みは任意で使える。
- 表示用カードはRunStateに登録しない一時モデルとし、一覧表示や確認だけでランのカード数・デッキ・セーブを変化させない。素材消費と本物のカード生成は確認後の錬成処理だけで行う。

## 2026-09-21: v0.3.0 作成カードを28種へ拡張

- ユーザーの試遊所感「多段攻撃、筋力・敏捷がなく、作れる種類が少ない」を受け、既存12種を維持したまま16種を追加した。全10素材ペアで複数の候補を提示する。
- 多段攻撃は7種。各ヒットを独立したカード由来の攻撃として処理し、筋力が各ヒットへ適用される。単体連撃、弱体後の連撃、毒付き連撃、ドロー付き連撃、全体連撃を用意した。
- 筋力を得るカードは6種、敏捷を得るカードは4種。そのうち《戦支度の霊薬》《霊質変成》は両方を得る。パワー主体に加え、廃棄付きの即時強化も用意した。
- 追加カードも通常素材2個で作成し、既存の容量・採取量・保存スキーマ・カードIDは変更しない。新formula IDは `.v1` で固定する。
- 数値は選択肢と構築の手触りを見るための強めの試作値。人手の通しプレイによる最終調整値ではない。

## 2026-09-21: v0.4.0 工房ノード・既存カード強化・素材相の継続

- 工房は通常Actの「？」候補だけから選び、元マップにある休憩所と同数を目標に配置する。休憩所自体や通常戦、商人、エリート、ボスは置換しない。候補は3行目以降かつ終盤3行を除外し、Act内で間隔が空くように選ぶ。
- 選んだ座標はラン状態に保存し、再表示やロードで再抽選しない。マップ上では金色の専用表示と「工房」ラベルを最初から見せる。ゲーム内部の部屋型は安定した遷移のため休憩所を使うが、通常の休憩所座標は変更しない。
- 工房に「既存カード強化」を追加する。未強化カードを一覧から選び、現在と強化後を並べて確認してから確定する。攻撃は鉄＋火薬、スキルは薬草＋エーテル、パワーは火薬＋エーテルを消費する。強化と素材消費は一括処理し、失敗時や取消時は消費しない。
- 戦闘終了時の次素材相をラン状態へ保存し、次の戦闘はその相から開始する。戦闘ごとの鉄固定を廃止し、相の偏りを抑える。初回だけは従来どおり鉄から始める。
- 工房配置座標と次戦闘の開始相は既存の素材箱セーブ領域へ追加する。既存セーブでは鉄・未配置から安全に初期化される。

## 2026-09-21: 工房ノードの頻度修正と休憩所機能の分離

- 工房数を「マップ上の休憩所と同数」から **1Actあたり最大2個**（`WorkshopPlanner.PerAct`）へ変更した。従来値は最終行が全て休憩所になるStS2のマップ構造により実際にはかなり大きく、中盤の「？」がほぼ全て工房になっていた。AGENTS.md 4.5の「通常のActでは1〜2回程度」から外れていたため設計値へ戻した。
- 候補の抽出条件に `MapPoint.CanBeModified` を追加した。ゲーム側が保護している行（1行目の通常戦、宝物庫／エリート行、最終休憩所行）を構造的に除外する。
- 工房ノードで休憩（回復）とカード強化ができないようにした。`MaterialBox.TryModifyRestSiteOptions` で該当ノードのみ選択肢を空にする。従来は本物の休憩所がそのまま使え、工房到達ごとに回復と「＋」強化が無償で付いていた。
- 上記に伴い `RestSiteRoom.Exit` を工房ノードに限ってスキップする。`RestSiteSynchronizer.BeforeLocalRestSiteExited` は選択肢が1つ以上残っている時にしか完了シグナルを立てないため、選択肢0個のまま退出すると `AfterAllRestSitesCompleted` が永久に待機する（実機スモークで再現）。本MODは単独プレイ前提のため、工房ノードでのみこの同期バリアを飛ばす。マルチプレイ対応時は専用の部屋型へ移す必要がある。
- 未決：工房ノードは依然として内部的に `RoomType.RestSite` を使っている。休憩所として扱う他のレリック・フックがあれば誤作動しうる。本来はイベント部屋か専用部屋型が正しく、エンチャント式の強化を入れる時に併せて整理する。

## 2026-09-22: v0.5.0 UI修正とレシピ閲覧の常時化

- 工房のマップアイコンが休憩所の焚き火のままだった原因は、パッチ位置が`NNormalMapPoint._Ready`だったこと。`UpdateIcon()`は`RefreshState()`からも呼ばれるため、ノードの状態が変わるたびに上書きされていた。テクスチャ差し替えは`UpdateIcon`のpostfixへ移し、「工房」ラベルの生成のみ`_Ready`に残した（毎回追加されるのを避けるため）。
- カードが一度ホバーすると縮小表示に戻らなかった原因は、`NCardHolder`が自前で`SmallScale`(0.8)と`HoverScale`(1.0)の間をtweenしているのに、こちらが`holder.Scale`を直接上書きしていたこと。ホバー解除時は常に0.8へ戻るため、指定した0.62には戻らない。表示倍率は親ノード側へ移し、ホルダーのスケール管理には触れないようにした（`MountCard`）。
- レシピ一覧を工房外でも閲覧できるようにした。素材ボックスから「レシピ一覧を見る」で同じカードギャラリーを開く。錬成ボタンは工房内でのみ表示し、工房外では「錬成はマップ上の工房で行います」と明示する。表示専用なので素材も乱数も消費しない。
- `scripts/update.ps1` / `install.ps1` のJSON読み込みに`-Encoding UTF8`を追加した。Windows PowerShell 5.1の`Get-Content -Raw`はBOMなしUTF-8をシステムANSIとして読むため、日本語を含むマニフェストが文字化けして`ConvertFrom-Json`が失敗していた。

## 2026-09-22: v0.5.1 工房を中盤と終盤へ配置

- 実プレイの指摘「ボス戦前に工房がほとんどなく、その階層で得た素材を使えない」に対応した。原因は配置ロジックの2点。
  1. 候補を`Row <= maxRow-3`で絞っており、行13・14を丸ごと除外していた。実マップ100件を計測したところ、改変可能な「？」は行2〜8と行10〜14に分布し、100件中81件で最も遅い候補が**行14**にあった（行9は宝物庫／エリート行、行1と行15はゲーム側の保護行）。つまり使える終盤候補を自分で捨てていた。
  2. 2個を`(i+1)/(count+1)`で均等配置していたため、2個目が行10〜12止まりになっていた。
- 変更後は「最後の1個は候補中で最も遅い行に置く」ことを保証し、残りはその行を基準に手前へ割り振る。固定の階数は埋め込まず、実際の候補分布から決める（AGENTS.md 4.5の「固定の階数をコードに埋め込まない」に従う）。
- 計測結果（100シード、Overgrowth）：中盤の工房は行5〜8（最多は行7）、終盤の工房は行11〜14（81件が行14）。
- AGENTS.mdの「Act1では数戦の後に初回錬成できる配置」との関係：初回工房は行5〜8で、従来（行5前後）よりやや後ろへ寄る。ユーザーの実プレイ判断を優先した。極端に遅くならないことは回帰テストで担保する。

## 2026-09-22: v0.6.0 全経路で工房を保証する配置（頂点カット）

- 要望：「どのルートからたどっても中盤と終盤で工房を踏めるようにしたい」。
- **AGENTS.md 4.5との相違**：本書は「到達可能とは有効経路が少なくとも1本あること。全経路での強制通過は要求しない。工房のない分岐を選ぶことは許可する」としていた。今回の指示はこれを上書きする（AGENTS.md 2「最新のユーザー指示を優先」）。
- マップは各行を必ず1つ通過する層構造なので、「全経路が踏む」＝開始からボスまでの**頂点カット**を工房にすることと等価。単純な行まるごとの変換は、実測で中盤・終盤の行の約90%が商人・休憩所・エリートのいずれかを含むため採用しない。
- 実装：最小カット（Edmonds-Karp、ノード分割）で解く。コストは「？」=1、通常戦=4、商人・休憩所・エリート・ボス・ゲーム側の保護ノード=変換禁止。これにより、商人や休憩所やエリートを潰さずに済む経路を自動で選び、「？」を優先しつつ必要な分だけ通常戦を使う。
- 帯は中盤（行3〜`MidBandEndRow`）と終盤（それ以降〜最終行）の2つ。各帯で独立にカットを求める。
- **保証できない場合は握りつぶさない**。帯内の全経路が保護ノードで塞がっていてカットが存在しないときは`WorkshopLayout.MidGuaranteed`/`LateGuaranteed`をfalseで返し、その帯は工房1個に縮退してログへ警告を出す。実測では100マップ中1件（終盤帯のみ）。
- **代償**：マップ上の工房ノードは1Actあたり平均6.6個になる。ただしプレイヤーが実際に通るのは中盤1個・終盤1個。通常戦の変換は平均2.44個/Actで、その分だけ戦闘機会と素材収入が減る。AGENTS.md 4.5の「通常戦を工房の都合で削らない」とは相反するが、全経路保証と両立しないため通常戦のみ許可し、商人・休憩所・エリートは維持した。

## 2026-09-22: v0.7.0 エリート・ボスの素材選択報酬

**背景**：撃破採取の上限はエリート3個・ボス4個だが、これらの部屋は敵が1〜2体しかいないことが多く、上限に届かない。AGENTS.md 4.4の選択報酬が未実装だったため、エリート・ボスほど素材が枯れるという逆転が起きていた。

**採用した判断**：

- `AbstractModel.TryModifyRewards` を `MaterialBox` で上書きし、エリートに1枠、ボスに2枠の`MaterialReward`を報酬一覧へ追加する。既存のゴールド・カード・レリック・ポーションは触らない。撃破採取の上限とは別枠で、片方が他方を減らすことはない。
- 候補の抽選は `MaterialOffers.Roll(runSeed, 枠ID)` による決定的な導出。ゲームの乱数ストリームを一切消費しないため、他の報酬・マップの乱数列をずらさず、「UIを開くだけでは乱数が進まない」「開き直し・ロードで再抽選しない」を同時に満たす。重みは`MaterialOffers.Weights`に分離し、重複なしで3個を引く。
- 枠の実体は`Reward`オブジェクトではなく`AlchemyState.Offers`（レリックのSavedProperty）に置く。ゲームは自前の`RewardType`しか直列化できず、BaseLibは未知の報酬をロード時に捨てるため、報酬画面で保存された場合に`Reward`側へ状態を持たせると枠が消える。`MaterialReward`はボタンにすぎない。
- 受け取りは既存の受領手順へ合流する。ボックス満杯時は`Grant`が`Pending`へ回し、交換か辞退を共通UIで解決する。
- 未解決の枠がある間は`EnterMapCoord`を差し止め、`Settled`（Pending・Offersともに空）を錬成・強化の前提条件に加えた。
- 選択UIは`NOverlayStack.Instance`を親にして報酬画面より後に追加する。`NRun`直下では報酬画面の背面に回る可能性があるため。工房・素材ボックス側の親は従来どおり`NRun`で、既存の挙動は変えていない。
- 枠IDは`{TotalFloor}:{RoomId}:reward{n}`。受領・辞退の両方で`Received`へ入るため、再入場・リロードで二重取得できない。
- セーブスキーマを1から2へ。schema 1は`Offers`が空の状態として読み込み、素材・受領IDを保持したまま移行する。未知スキーマは従来どおり拒否し、初期化はしない。

**未決事項**：

- ボスの希少確定枠（AGENTS.md 4.4）は希少素材そのものが未実装のため保留。今回はユーザー確認のうえ、2枠とも通常素材の3択とした。希少素材を実装するM3で、片枠を希少3候補に切り替える。その際`MaterialOffers.Weights`に希少分を足し、希少枠だけ別プールを引く形になる見込み。
- 枠数（エリート1・ボス2）と候補数3は試作値。通しプレイでの素材経済はまだ測っていない。
- 素材報酬のアイコンは`burning_blood.png`の流用。専用アイコンは未作成。
- 通常戦で敵が1体のときに撃破採取が2個に届かない件は、今回の変更では解消していない。エリート・ボスのみを対象とした。

## 2026-09-22: v0.8.0 通常素材を炉の廃棄時に採取

**背景**：敵撃破時の採取では、同じ戦闘でも敵が1体か複数かによって得られる素材数が変わり、デッキ構築と無関係な遭遇編成の差が素材経済へ強く影響していた。ユーザーの最新指示により、撃破採取を廃止し、《炉の起動》でカードを廃棄した瞬間へ採取タイミングを移す。

**採用**：

- 《炉の起動》で選択したカードの廃棄が成功した直後、確定済みの現在相に対応する通常素材を1個得る。
- 《炉の起動》は従来どおり戦闘全体で2回まで。敵の数・召喚・分裂・復活は通常素材の獲得数へ影響しない。
- カード選択の取消、廃棄失敗、3回目以降の起動では素材を得ない。
- エリート1枠・ボス2枠の素材選択報酬は、《炉の起動》による採取とは別枠で維持する。
- 素材相のターン循環と戦闘をまたぐ継続は維持する。どのターンに廃棄するかが素材選択になる。

**互換性**：ラン保存の`AlchemyState`スキーマは変更しない。戦闘中だけ存在する`HarvestCombat`から敵ID・撃破上限を削除するため、ゲームが対応しない戦闘途中保存には依存しない。

## 2026-09-22: v0.9.0 希少素材と恒久加工

**採用**：希少素材は回収可能な装着品ではなく、カードへ効果を刻む消費触媒とする。水銀は保留、星砂はゲーム標準の`BaseReplayCount`によるリプレイ1回、虚無結晶はコスト1減少＋廃棄。対象は錬成カードのみで、1枚につき1種類、取り外し・回収・上書きは不可。既存の錬成カードにも工房で後付けできる。カード複製が加工結果を引き継いでも希少素材を回収できないため、素材は増殖しない。

ボスの`reward1`を希少確定枠とし、初期3種をすべて提示する。エリート枠とボスの非保証枠は、ランシードと枠IDだけで決まる25%の判定に成功した場合、通常素材3候補の1個を希少素材へ置き換える。1枠へ希少素材が複数混ざらないため、ボス確定枠の価値を保つ。確率は試作値。

通常素材と希少素材は容量10を共有する。報酬候補と保留受領を`MaterialChoice`で型付きにし、交換相手には通常・希少の両方を選べる。セーブスキーマを3へ上げ、schema 1/2の整数候補を通常素材の`MaterialChoice`へ明示的に移行する。未知・破損スキーマは初期化せず拒否する。

工房訪問ごとの加工1回制限は採用せず、試作では所持する希少素材が続く限り加工できる。希少素材の入手機会が少ないため、まずAct通しの取得・使用件数を測り、必要なら訪問制限を追加する。

## 2026-09-22: v0.9.0 レシピカードの診断文残留修正

レシピ一覧の一部に`If you can read this, there is a bug.`が残った原因はローカライズ欠落ではなく、プールから取得した`NCard`の内部ラベルが準備される前に`UpdateVisuals`が呼ばれるタイミング競合だった。カード強化や再ログインで見えるようになるのは、その操作が再描画を発生させるため。初回更新に加え、ノードがツリーへ入った後のdeferred更新を行い、表示状態も`Visible`へ明示する。

## 2026-09-22: v0.10.0 通常カード報酬の廃止と採取量2倍（着手）

**背景**：ユーザーの判断は「初期デッキの弱プールが辛いのは最初期だけで、そこは乗り越えられないことはない。カード報酬を受け取らず、代わりに素材の入手数を2倍にし、錬成で作れるカードを70枚程度まで充実させて対応したい」。あわせて、コモン2素材・アンコモン3素材・レア4素材という消費量の目安と、「素材はデータの数値ではなく、別デッキのようにカードとして存在し、商人は素材か無色カードを売る」という構成変更の要望を受けた。

**AGENTS.mdとの相違**：8章「やってはいけないこと」は「通常カード報酬を勝手に廃止し、工房到達前の成長を失わせる」を禁じている。今回はユーザー本人による明示指示であり「勝手に」ではないため、2章の優先順位（最新のユーザー指示＞採用方針）に従ってこの回だけ上書きする。工房到達前の成長は、カードではなく素材2倍による錬成前倒し（後述の70枚拡張が前提）で担保する狙い。

**今回実装した範囲（Phase 1）**：

- `MaterialBox.TryModifyRewards` で `CardReward` を全戦闘（通常・エリート・ボス）の報酬から除去する。素材選択枠（エリート1・ボス2）はそのまま維持。
- `AlchemyState.YieldPerEvent = 2` を新設し、1回の採取イベントにつき同じ素材を2個渡す `AlchemyState.GrantHarvest` を追加した。内部では基準ID（`id`）で1個目を渡す既存の`Grant`を呼び、2個目以降は`id:bonus{n}`という別IDで追加の`Grant`を呼ぶ。理由：`Received`集合や容量・保留（Pending）・満杯時の交換ロジックを一切変更せずに済み、単発の既存テスト資産（満杯・保留・交換・再入場防止）をそのまま再利用できる。報酬枠の再提示防止は基準IDの`Received`登録に依存しているため、基準IDのGrant呼び出しは変えていない。
- 《炉の起動》（`MaterialBox.GrantFromFurnace`）とエリート・ボス報酬受領（`AlchemyState.TakeOffer`）の両方を`GrantHarvest`経由に変更。炉の起動は戦闘全体2回のまま、1回あたりの採取量が1個→2個になる（戦闘全体で最大4個）。
- カード・relicのローカライズ文言（素材ボックス、炉の起動）を新しい採取量・カード報酬なしの説明に更新。
- `tests/Alchemist.Core.Tests/Program.cs`を新しい採取量に合わせて更新（`YieldPerEvent`を参照する形にし、満杯時に保留が2件に分割されること・2回に分けて交換で解決できることを検証）。全223件成功、`scripts/build.ps1`のビルドも成功。

**未実装・未決（Phase 2以降、着手前にユーザー確認が必要）**：

- **錬成カード70枚への拡張**：通常素材4種の順不同・重複可の組み合わせは、2個で10通り、3個で20通り、4個で35通り（合計65通り）。ここに希少素材を使う特殊レシピを5種足して70枚とする案の実現性は確認済みだが、実際のカード内容（コモン/アンコモン/レア割付、個々の効果・数値）は未着手。現行の`ForgeCatalog`は素材2個のみの28種構成であり、3個・4個消費という新しい入力形式は`AlchemyState.CanCraft`/`Commit`/`ForgeFormula`のモデルを拡張する必要がある（現状は`First`/`Second`の2枠固定）。
- **素材の「別デッキのカード」化**：現在の`AlchemyState.Counts`（int配列のインベントリ）を、ゲームの`CardModel`インスタンスとして戦闘デッキとは独立した永続領域に保持する形へ作り替える構想。StS2の`CardPile`は基本的に戦闘スコープ（Deck/Hand/Draw/Discard/Exhaust）のため、ラン全体で持続する非戦闘カード集合をどう表現するか（専用の永続ピール／ビューア画面の自作か、既存のデッキ閲覧画面を転用するか）は未調査。セーブ形式・容量UI・工房UIの大部分に影響するため、着手前に実現方式を確認する。
- **商人の商品構成変更**：商人は現状`MerchantInventory`標準実装（キャラ専用カード4枠・無色カード2枠・レリック3枠・ポーション3枠・カード除去）をそのまま使っており、本MOD独自の商品枠は未実装。「素材か無色カードを購入できる」形にするには、キャラ専用カード4枠を素材購入枠へ置き換える（`MerchantInventory`は`sealed`ではないクラスだが差し替え方法は未調査）か、別UIを工房または商人ノードに追加するかの方式を決める必要がある。

Phase 2着手前に、上記3点（レシピの3・4素材化、素材のカード化方式、商人の商品差し替え方式）をユーザーに確認してから実装する。

## 2026-09-22: v0.9.1 カード・素材の双方向錬成UI

従来の完成カード一覧を「カードから探す」として維持し、「素材から作る」を追加する。素材側は通常素材2枠を順に埋め、`Recipes.FindAll`で順不同の該当候補を表示する。同種2個も許可し、所持数を超える配置はできない。スロットは入力プレビューに限定し、カード確定前に素材・乱数・ラン状態を変更しない。確定操作は従来の`CommitAsync`へ合流するため、消費・カード追加・失敗時ロールバックは両UIで同じになる。
