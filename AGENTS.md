# 錬金術師MOD — 開発エージェントへの指示

## 1. 目的と対象

Slay the Spire 2（StS2）向けに、素材採取・カード錬成・改造・ポーション調合を軸とするプレイアブルな錬金術師MODを実装する。

中核となる体験は「戦闘中は相を切り替えて戦いながら炉で素材を採り、素材を今使うか次の工房まで蓄えるかを選び、通常のカード報酬で組んだデッキを工房で特殊に加工する」こと。ポーションだけを量産するキャラクターにはしない。

- **実装対象はStS2。** StS1の同時実装はスコープ外。
- StS1への将来の移植に備え、ゲームに依存しないルールとゲームAPIへの接続を分離する。ただし、両作品で同じソースやバイナリが動くとは想定しない。
- このファイルは開発先リポジトリのルートに置いて使う。既存プロジェクトでは、既存の指示・構造を確認して統合する。
- 作業報告は日本語。識別子・ファイル命名は既存プロジェクトの規約に従う。

## 2. 仕様の扱い

### 2026-09-26 カード軸・生命軸・素材経済（`docs/design-axes.md`）

`docs/design-axes.md`は2026-09-26のユーザーとの設計相談の決定をまとめたもので、本書より新しい。本書と食い違う場合は`design-axes.md`を優先する。決定・試作値・未決の区別も同書に従う。

- 相（属性）はキャラ全体の土台。その上に**相転移軸**（1ターンに多く転移して強くなる）と**生命軸**（ドレインとホムンクルスで溜め、ロマン砲で決める）の2軸と、両方にまたがる橋渡しを置く。素材は軸ではなく、全体に散らばるラン内資源。
- 素材供給は《炉の起動》1回2個・1戦闘2回・容量20（通常・希少合算）。v0.16.0で1個・容量10へ戻したのは古い指示書を参照した誤り。**コードへの反映は、素材消費カード・錬成コストの見直しと同じリリースで行う**（供給だけ先に増やすと素材が余る）。v0.21.0でコードにも反映済み（エリート・ボスの選択報酬は1個のまま）。

#### 設計原則（design-axes.md 2、決定）

1. 画面で確認できる情報は参照してよい（現在相、デバフ、ホムンクルスHPなど）。
2. 履歴の参照はバニラ並みなら許容する（「このターンの相転移回数」などは専用UIなしでよい）。
3. **揃える難しさを代償にしたハックは許す。** コスト・レアリティ・手札の噛み合いを要求する強い組み合わせは、強すぎても修正しない。エージェントが「強すぎるので修正した」と独断でコンボを潰さないこと。
4. 塞ぐのは2種類だけ：準備なしで毎回成立する組み合わせと、無限ループ。
5. 相転移でドロー枚数を増やさない。転移とドローの循環はループの燃料になる。
6. 相転移の基本効果は変えない（地＝ブロック、水＝脱力、火＝ダメージ、風＝ドロー）。
7. 「転移1回ごとに○○」の報酬は弱めにする。相転移軸の本体は「1ターンの転移回数で伸びる報酬」に置く。
8. 錬成・心核などの強化を相ごとに一律にしない。相ごとに価値が釣り合うよう個別に設計する。

#### 実装前チェックリスト（design-axes.md 10.1）

新しい仕組みや軸を実装する前に、以下が揃っているか確認する。**揃っていなければ、実装せずにユーザーに確認する。** 数値、残りのカード、実装の詳細は試作値として進めてよい。

- その仕組みの役割（何のためにあるか、どの軸に属するか）
- 守るべき原則・やってはいけないこと
- 決定・試作値・未決の区別
- 手本になるカード数枚

### 2026-09-24 新設計（v0.15.0〜）

本書は`docs/design-decisions.md`の「v0.15.0 四元素・通常カード報酬・4設備工房」と「v0.16.0」に合わせて改訂済み。通常カード報酬を維持し、素材とは別の戦闘状態として地・水・火・風の相を持ち、工房は錬成・改造・調薬・付与を1訪問につき各1回ずつ使う。「カード報酬廃止」「ターンごとの素材相の自動循環」「工房で資源が続く限り連続錬成」は開発履歴であり、現行仕様ではない。

相転移は「どの相へ移ったか」だけで効果を決める。移動元との組み合わせ表、同相維持ボーナス、ターンごとの回数制限は、覚える量を増やすため採用しない（2026-09-24 ユーザー確認）。

本書の「採用方針」は維持する。「試作既定値」は開発を止めないための仮定であり、会話で確定した数値ではない。「未決事項」は勝手に完成仕様として扱わない。

仕様の判断は、最新のユーザー指示、本書の採用方針、記録された設計判断の順に参照する。過去のアイデアを、後の修正より優先して復活させない。

小さく可逆な判断は、理由と仮定を記録して進める。中核体験を変える判断、セーブ互換性の破壊、既存報酬の廃止は、必要な調査と選択肢の整理を済ませてからユーザーに確認する。

### 採用方針

| 項目 | 方針 |
|---|---|
| 相 | 素材とは別の戦闘状態（無相・地・水・火・風）。属性カードの使用で変化し、ターンをまたいで保持、戦闘終了でリセット |
| 相転移 | 異なる相へ移った時だけ、移動先の相で決まる効果が1つ発動する（地＝防御、水＝脱力、火＝攻撃、風＝ドロー）。無相からの最初の相では発動しない |
| 炉採取 | 初期レリックが各戦闘の最初に《炉の起動》を配る。カードの廃棄に成功した時の現在相に対応する通常素材を2個得る（1戦闘2回、最大4個） |
| 通常カード報酬 | ゲーム標準のカード報酬を維持する。基本的なデッキ構築は報酬、特殊な加工は工房が担う |
| 素材ボックス | デッキと独立したラン内インベントリ。戦闘をまたいで保持 |
| 通常素材 | カードの基本効果を形作る消耗資源 |
| 希少素材 | 炉採取からは出ない。カードの振る舞いを変えるための資源 |
| エリート報酬 | 通常の炉採取とは別に、通常・希少素材を含む抽選から3候補を提示し、1個選ぶ。希少出現は保証しない |
| ボス報酬 | 3択から1個を選ぶ枠を2つ提示。うち1枠は3候補すべて希少素材で、選んだ1個が希少確定 |
| 工房 | 休憩所と別の専用ノード。既存の階層内に配置し、マップ上で事前表示 |
| 工房の処理 | 錬成・改造・調薬・付与の4設備を、1訪問につき各1回まで使える。資源が残っていても同じ設備は再使用できない |
| ガラス瓶 | 当面は採用しない（2026-09-24 ユーザー判断）。調薬は通常素材1個でポーション1本を作る |
| 初期デッキ | Strike 3、Defend 3、地相カード1、水相カード1、《即席錬成》の計9枚（試作） |
| 即席錬成 | 所持する通常素材1個を、戦闘中だけ使える一時カードへ変える。工房用の資源を今使うかどうかの判断を作る |
| カードレシピ | 予測可能な基本合成と、特定の組み合わせによる固有レシピのハイブリッド |
| ポーションレシピ | 固定レシピを基本とする |

ボスの非保証枠を通常素材だけにするか、通常・希少の混合にするかは未決。試作ではエリートと同様の混合プールを使う。この場合、両枠から希少素材を得ることもある。「片方はレア確定」は「希少は必ずちょうど1個」の意味にはしない。

## 3. 実装前の調査

**StS2のMOD API、ローダー、対応バージョン、言語・ビルド環境、パッケージ形式は、実装時点の最新情報と実際の対象ゲーム環境で確認する。** この文書は具体的なAPI名や開発環境の有効性を保証しない。

1. 既存リポジトリの指示、構成、依存関係、ビルド・テスト方法を読む。
2. 対象ゲームのビルド番号、ローダーと依存MODのバージョン、利用できる開発環境を確認する。
3. 公式資料、使用するローダーの配布元・ソース・リリース情報、同じゲーム版で動く最小サンプルを優先して調べる。
4. キャラクター登録、ターン境界、敵の死亡と戦闘終了、報酬選択、マップ生成、商人、カード・ポーション生成、保存・読込、UIと翻訳の拡張点を確かめる。
5. 最小MODをビルド・起動し、採用する接続方法が実環境で機能することを確認する。
6. 調査日、参照URL、対象版、確認できた事実、未検証項目を `docs/research.md` に記録する。

StS1向けのクラス名、パッチ方式、ライブラリをStS2へそのまま持ち込まない。将来StS1へ移植するときも、その時点のAPI差分を再調査する。記憶や他作品の例だけで架空のAPIを実装しない。

資料の「最新」と利用中のゲーム版が食い違う場合は、利用中の版で動く組み合わせを優先し、差分を明示する。依存関係は再現できるようバージョンを固定する。

ゲーム本体・有料アセット・他MODのコードを無断で同梱しない。参照するコードと素材は利用条件を確認する。

## 4. ゲームルール

### 4.1 相・相転移と炉採取

- 相は **無相・地・水・火・風**。素材（鉄・薬草・火薬・エーテル）とは別概念で、素材の所持とは独立した戦闘状態として管理する。
- カードは自身の相（属性）を持つ。錬成カードは素材構成から既定の相を導くが、定義で上書きできる。対応の基本は鉄＝地、薬草＝水、火薬＝火、エーテル＝風。
- 各戦闘は無相で開始する。属性カードを使うとその相になり、相はターン終了で消えず、戦闘終了でリセットする。
- 現在相と異なる相へ移った時だけ相転移が起きる。同じ相の連続使用、リプレイによる同じプレイの再処理では起きない。無相から最初の相へ移る時に発動するかは設定で切り替えられる（既定は発動しない）。
- 相転移の効果は移動先の相だけで決まる4種類（地＝ブロック、水＝脱力、火＝ダメージ、風＝ドロー）。判定と効果の解決は1か所に集約し、各カードへ同じロジックを書かない。パワー・レリック・カードの改造状態から、効果量の変更・封印・発動後の反応を追加できるようにする。
- 現在相を戦闘中に常に表示し、相転移時には視覚的なフィードバックを出す。
- 素材は《炉の起動》で選択したカードの廃棄が成功した直後の、確定済みの現在相で決まる。無相では《炉の起動》を使用できない。
- 《炉の起動》1回につき通常素材2個（design-axes.md 0.1。コード反映は素材消費カードと同じリリース）。カード選択の取消や廃棄失敗では増やさない。使っても素材を得られない状況では、使用不可として表示し、空撃ちで回数を失わせない。
- 炉の起動は戦闘全体で2回まで。パワーの複製・再適用や通知の再送で回数を増やさない。
- 敵撃破では通常素材を得ない。敵の数、召喚、分裂、変身、復活によって素材獲得数が変わらないようにする。
- 戦闘を長引かせるだけでは素材が増えない。ダメージ閾値、ブロック破壊、デバフ付与による追加採取はMVPに入れない。
- 相を進める・維持する・任意の相へ変えるカード、相転移を2回起こす効果は拡張候補。初期実装は属性カードによる相転移と、炉による有限回数の採取を成立させる。

炉採取は通常戦・エリート・ボスのいずれも2回まで（1回2個で最大4個）。エリート・ボスの選択報酬はこの上限と別枠にする。

### 4.2 素材ボックス

- カード、手札、山札、捨て札、廃棄札のどれにも素材を混ぜない。
- 所持数、容量、素材の説明、入手元を確認できるUIを用意する。
- 試作容量は通常・希少素材・空のガラス瓶を合算して20個（design-axes.md 0.1）。同じ種類の素材も個数分の容量を使う。完成ポーションはゲーム標準のポーション枠へ入れる。
- 満杯のときは受け取り辞退または既存素材との交換を選べるようにし、希少素材を含め無断で破棄しない。
- 戦闘中の満杯通知で戦闘進行を止めない。獲得権を保留し、戦闘後に受け取りを解決する。保留領域は自由に使える追加倉庫にはしない。
- 戦闘後の受け取りが未解決のまま次の部屋へ進めない。辞退・交換と選択報酬を処理順付きで管理する。
- ラン終了時に素材はリセット。ランをまたいだ資源持ち越しは行わない。

### 4.3 通常素材・希少素材

通常素材の意味は、鉄＝物理攻撃・防御、薬草＝毒・弱体など、火薬＝高火力・全体攻撃、エーテル＝ドロー・エナジーなどを基本とする。HP回復やエナジー増加は循環による無限稼ぎを検証してから採用する。

希少素材は炉採取の対象に含めない。初期の3種は、水銀＝保留、星砂＝リプレイ1回、虚無結晶＝コスト1減少＋廃棄。単純な威力加算ではなく、カードの使い方を変える。

希少素材は工房で消費する恒久加工触媒とし、錬成カード1枚に特殊効果を1つ刻む。既に作った錬成カードにも後付けできる。初期実装では取り外し・回収・上書きは不可。カード複製は加工結果を複製しても素材を生成しないため、素材経済上の増殖にはしない。

### 4.4 エリート・ボスの選択報酬

- 通常のカード・ゴールド・レリック等の報酬は維持し、素材選択を追加する。既存報酬の置き換えは未承認。
- エリート：1枠、3候補、選択した素材を1個取得。
- ボス：2枠、それぞれ3候補から1個取得。希少保証枠には通常素材を混ぜない。
- 候補の生成と選択結果を保存する。開き直しやロードで再抽選しない。
- 同じ枠では異なる素材を優先し、抽選は重み付き・重複なしを試作既定とする。MVPの希少プールも最低3種類用意する。
- 枠をまたいだ同種素材の出現は許可する。希少出現率と重みはデータで調整可能にする。
- 1枠を2回受け取れないよう、戦闘ID・報酬枠ID・受け取り状態を保持する。
- 素材上限に達した戦闘でも選択報酬は発生する。ボックス満杯時は共通の受け取り手順で解決する。
- 報酬の辞退は許可する。希少保証は提示される内容の保証であり、ユーザーによる辞退を禁止しない。

### 4.5 工房ノード

- マップの総階層数を増やさず、原則として「？」ノードの一部を工房に置き換える。
- 通常戦、休憩所、商人、エリート、ボスのノードを工房の都合で削らない。
- 工房は最初から専用アイコンと説明を表示する。「？」に入ってから偶然判明する方式を主導線にしない。
- 通常のActでは1〜2回程度を目標とし、少なくとも前半〜中盤に到達可能な工房を置く。Act1では数戦の後に初回錬成できる配置を目指す。
- 「到達可能」は試作では開始位置から選択できる有効経路が少なくとも1本あること。全経路での強制通過は要求しない。ユーザーが工房のない分岐を選ぶことは許可する。
- 固定の階数や「49階」をコードに埋め込まない。対象版のAct構造を調べ、特殊な終盤エリアは通常Actと分けて扱う。
- 候補不足のときは、既存の接続を維持できる配置を再探索する。通常戦等の無断置換で取り繕わず、生成制約を満たせないケースを記録・検出する。
- 工房には **錬成・改造・調薬・付与** の4設備がある。1訪問につき各設備を1回まで使え、別の設備の使用を妨げない。素材が残っていても同じ設備は再使用できない。工房を退出したら、次の工房まで恒久加工はできない。
  - 錬成：素材を消費して、通常のカード報酬より特殊なカードを1枚作る。
  - 改造：既存カード1枚に工房固有の恒久効果を付ける。休憩所の通常アップグレードとは別物で、強化済みのカードも対象にできる。
  - 調薬：素材を消費してポーションを1本作る。
  - 付与：希少素材を消費して、錬成カード1枚に恒久的な特殊効果を刻む。
- 工房UIでは各設備を「使用可能」「使用済み」「素材不足」で区別して表示する。
- 希少素材による付与は、カード1枚につき特殊効果1つまで。同じカードへの反復加工はできない。

### 4.6 ガラス瓶とポーション調合

**当面は瓶なし（2026-09-24 ユーザー判断）：** 調薬は、ガラス瓶を使わず通常素材1個でポーション1本を作る。以下の瓶に関する項目は、将来瓶を再検討する場合の方針として残す。瓶の有無にかかわらず、取消時の非消費、ポーション枠が満杯の時の扱い、部分成功の禁止は守る。

- 商人は通常の商品に加え、錬金術師用の空のガラス瓶を扱う。
- 瓶は消耗品であり、素材ボックスに入れる。購入成功時にゴールドと在庫を一度だけ更新する。
- 試作価格は30G、在庫は訪問につき1本。いずれも調整用の仮値で、対象ゲームの相場に合わせて検証する。
- 調合は「ガラス瓶1個＋固定レシピの素材」を消費し、完成ポーション1本を作る。
- カードとポーションは通常素材を共有し、恒久的なデッキ強化と一時的な戦力の間に選択を作る。
- 完成品の効果・消費素材・行き先を確定前に表示する。ポーション枠が満杯なら交換か取消を選べるようにする。
- 取消時は素材も瓶も消費しない。消費後に完成品だけ消える部分成功を作らない。
- 使用済みポーションからの瓶回収、特殊な瓶、瓶の永久使用はMVPに入れない。

### 4.7 スターターデッキ

- 試作はStrike 3、Defend 3、地相カード1、水相カード1、《即席錬成》1の9枚。対象版の標準カードとの互換性を確認する。
- キャラクター固有の仕組みを特定のカードを引くことに依存させない。炉は初期レリック（素材ボックス）が各戦闘の最初に《炉の起動》を2枚配る。旧《携帯錬金炉》は旧セーブ読込用に定義だけ残す。
- **毎ターン無制限に廃棄して素材を得る方式は採用しない。** デッキ圧縮と素材生成の無限循環を防ぐ。
- 炉の効果は「この戦闘中2回まで手札1枚を廃棄し、現在相の通常素材2個を得る」。使用回数は戦闘全体の共有上限として扱い、複製や再適用で増やさない。
- 《即席錬成》は所持する通常素材1個を消費し、対応する相の0コスト・廃棄の一時カードを手札に加える。所持0個の素材は選べない。
- 戦闘中の廃棄とマスターデッキからの永久削除を混同しない。
- 初期素材は試作では0個。初期HP、初期レリック、その他のキャラクター値は対象版を調査して仮設定を記録する。

### 4.8 カード報酬プールと錬成レシピ（v0.17.0〜）

プール規模の目標、錬成の段（2素材・3〜4素材・5素材）、心核の相ごとの設計、改造の方向は`docs/design-axes.md` 7〜8章が新しい。以下はv0.17.0時点の構成。

役割を分ける。**カード報酬＝相を1つ持つ基本カード、工房の錬成＝報酬では得られない相操作カード**。

- 報酬プールと錬成カードの合計は、他キャラクターのプール（約91枚、Basic 4・Common 20・Uncommon 38・Rare 27・Ancient 2、アタック約32・スキル約39・パワー約20）に揃える。試作は報酬75枚＋錬成10種＝85枚。
- 報酬プール76枚：Common 20・Uncommon 33・Rare 23、アタック29・スキル33・パワー14。地・水・火・風が各16枚、無相が12枚。Commonにパワーを置かない。
- 報酬・商人に出すカードはCommon〜Rare、出さないカード（錬成専用、トークン、素材表示用）はEvent・Tokenにする。報酬と商人はCommon・Uncommon・Rareの中でしか抽選しない。
- 商人はアタック2・スキル2・パワー1の枠でキャラクターカードを並べるため、プールには各タイプが必ず必要。
- 錬成は順不同の通常素材2個で10種。同じ素材2個は「その相への相転移の効果を強化する」パワー、違う素材2個は「効果の後に1つ目の相、次に2つ目の相へ入る」複相カード。同じ入力とルール版なら同じ結果を返す。
- 相転移の効果は「移った先の相」だけで決まる原則を錬成でも守る。複相カードは転移を最大2回起こすだけで、新しい組み合わせ表は作らない。
- 生成カードには安定した定義IDを持たせる。表示名やテキストを保存時の識別キーにしない。旧フォーミュラ65種はv0.18.0で削除した（ユーザー判断）。
- カード本文に「廃棄」「保留」などのキーワード名を書かない。ゲームが自動で付けるため二重表示になる。
- ゲームに登録するポーションは必ずいずれかのプールに入れる。報酬・商人に出さないものは`TokenPotionPool`へ。
- 希少素材の付与は、錬成カード（新旧とも）に1枚1つまで。
- 工房の既存カード改造は対象・回数・付与上限を明示する。無制限に同じ強化を積み重ねない。
- 分解の返却量は未決。作成と分解の往復だけで素材・希少素材・カード削除機会を増やせない設計にする。

レシピ帳は拡張候補。採用する場合、ランをまたいで残るのは発見した知識だけとする。試作では確定前の結果をすべて表示する。

## 5. アーキテクチャ

以下は責務の分け方であり、実在するStS2のAPI名ではない。言語・ディレクトリは調査結果と既存プロジェクトに合わせる。

| 層・責務 | 内容 |
|---|---|
| 定義データ | 素材、レシピ、ベースカード、報酬重み、価格、容量、配置条件、翻訳キー |
| ドメイン | 相と相転移の規則、在庫、レシピ解決、報酬候補、工房処理、容量・消費・回数制限 |
| ラン状態 | 素材、未受領素材、確定済み報酬、生成カード、訪問履歴、受け取り済みID |
| 戦闘状態 | 現在相、ターン処理済みID、炉の起動状態、戦闘全体の使用回数 |
| StS2接続 | イベント購読、ゲームの乱数・保存・カード・報酬・マップ・商人APIとの接続 |
| 表示・操作 | 素材ボックス、現在相表示、工房、レシピプレビュー、選択報酬 |
| 永続プロフィール | 必要ならレシピ発見記録。ランの資源とは別に保存 |

- ドメイン層からゲーム画面やゲーム固有クラスへ直接依存しない。ルールをゲーム起動なしでテストできるようにする。
- UIは共通の処理サービスを呼ぶ。画面側に素材消費やレシピ計算を重複実装しない。
- 通知型イベントと資源を変更する操作を区別し、同じ通知の再配送を安全に無視できるようにする。
- プレビューは読み取り専用。乱数消費・素材消費・実カード追加を行わない。
- 乱数は対象ゲームのシード付き機構を調査して利用する。可能ならMOD用の独立ストリームを使い、他の報酬・マップの乱数列を不要にずらさない。
- レシピと報酬には安定IDを付ける。列挙順やローカライズされた名前に結果を依存させない。
- 工房の確定操作は「検証→コミット」。失敗時は消費も成果物追加も行わず、再入力可能にする。
- セーブにスキーマ版を付ける。移行できないデータを黙って初期化しない。バックアップ・移行・明示的な非互換表示のいずれかで保護する。
- 対象ゲームが保存を許可するタイミングを調査する。戦闘中保存が非対応なら対応を約束せず、可能な保存境界で状態の整合性を保証する。
- 他キャラクターのランでは錬金術師用の報酬・商人・マップ改変を有効にしない。
- MVPは単独プレイ。協力プレイ等を対象にする場合は、所有者・報酬の共有範囲・同期・乱数の決定権を別途設計し、未検証の対応を宣言しない。

## 6. マイルストーンと完了条件

各段階は、小さく動く成果物と検証結果を残してから進める。大きなカードプールや完成アートを先に作らない。

### M0：対象環境の確認と最小起動

- 対象版・ローダー・依存関係を記録し、再現可能なビルド手順を作る。
- 最小MODが起動し、錬金術師の登録と9枚の初期デッキを確認する。
- 完了条件：実環境の起動証拠、または実行できない具体的理由と未検証範囲が記録されている。ビルド成功だけを起動成功と報告しない。

### M1：採取から錬成までの最小ループ

- 4種の相と相転移、炉の廃棄時採取、素材ボックス、容量解決を実装する。
- 開発用に入れる工房と少数の基本レシピを用意する。
- 炉と《即席錬成》の有限回数の試作効果を実装する。
- 完了条件：戦闘→素材獲得→工房で消費→作ったカードを次の戦闘で使う流れが成立する。

### M2：通常のランへの組み込み

- マップへの工房配置と事前表示、エリート3択1枠、ボス3択2枠を実装する。
- 商人の瓶販売と固定ポーションレシピを実装する。
- セーブ・ロード、報酬受け取りの重複防止、工房4設備の各1回制限を完成させる。
- 完了条件：開発用の直接移動に頼らずAct1を遊べ、ボスの希少保証枠を受け取り、次Actへ資源を引き継げる。

### M3：ハイブリッドと希少加工

- 基本レシピ文法、少数の固有レシピ、希少素材による挙動変更を実装する。
- 希少素材の消費／装着・回収方式、分解の返却量、加工枠を設計記録に確定する。
- 必要なら改造・分解とレシピ帳を追加する。
- 完了条件：全基本入力で結果が一意になり、プレビュー・実行・保存後の効果が一致する。合成・分解・複製による資源増殖がない。

### M4：通しプレイと配布準備

- 対象版の通常ランを通してプレイし、工房アクセス、素材経済、初期デッキの生存性を調整する。
- UI・日本語表示・入力操作・エラー表示を確認する。
- インストール、起動、依存関係、対応版、既知の制限、アンインストール時のセーブ注意点をREADMEへ記載する。
- 完了条件：再現可能な配布物と検証記録がある。外部公開はユーザーの指示がある場合に行う。

## 7. テスト条件

### 自動検証するルール

- 相：無相開始、最初の相では発動しないこと、同相では転移しないこと、移動先ごとの効果、ターンをまたぐ保持、戦闘終了時のリセット、改造による効果量の変更。
- 採取：廃棄成功直後の付与、選択取消、2回上限、パワーの複製・再適用、敵数によらないこと、追加報酬との独立性。
- 在庫：0個、容量直前、満杯、同種複数、辞退、交換、保留の解消。個数が負にならないこと。
- エリート：3候補から1個だけ取得し、希少素材も抽選対象となること。
- ボス：2枠とも3候補で、保証枠の全候補が希少であること。片方だけ受領した状態からの再開でも重複・再抽選しないこと。
- 乱数：同じシード・入力・版で候補が一致し、UIを開くだけでは乱数列を進めないこと。
- レシピ：基本入力の全列挙、順序の正規化、固有レシピ優先、不足・不正入力の拒否、プレビューと成果物の一致。
- 工房：各設備が1訪問1回であること、別設備を妨げないこと、資源不足、取消、二重クリック、途中失敗、再入場、使用状態の保存。
- ポーション：瓶なし、素材不足、ポーション枠満杯、交換取消、成功時に瓶と素材が一度だけ減ること。
- 炉・即席錬成：使用上限、無相での使用不可、複製・再適用、1回の成功につき決まった個数（炉は2個、即席錬成は1個）だけ増減すること、所持0個の素材を選べないこと、戦闘終了時のリセット。
- 保存：在庫、生成カードと強化、工房の状態、未受領報酬を復元できること。対応する旧スキーマの移行と未知IDの扱い。
- 分解・回収を実装したら、作成→改造→分解の循環で資源が純増せず、コピーから希少素材を回収できないこと。

マップ生成は複数シードで検証する。試作の目安は100シードとし、元マップと階層数が同じ、有効な接続が残る、禁止ノードを置換しない、通常Actの早い位置に到達可能な工房があることを機械的に検査する。失敗シードは再現可能な形で記録する。

### 実ゲームで検証する項目

- 《炉の起動》で選択したカードの廃棄と素材付与の順序、取消時・廃棄失敗時の挙動。
- 報酬画面・商人・工房のキャンセルと再表示、ゲームが許す地点での保存・再開。
- 生成カードのコピー・強化・戦闘後の復帰で、元カードの定義と装着資源が壊れないこと。
- 工房アイコン、現在相、素材数、長い日本語、ツールチップが判読でき、対応する入力方法で操作できること。
- 他キャラクターのマップ・報酬・商人への副作用がないこと。
- Act1の数戦から最初の工房まで到達でき、攻撃・防御・範囲攻撃等の必要な選択肢が素材の偏りで恒常的に失われないこと。

バランス調整では初回工房までの戦闘数、素材取得・廃棄数、錬成回数、瓶の購入・調合数、採用レシピの偏り、意図的な戦闘引き延ばしをローカルで記録する。件数を報告し、少数の試遊から「バランス調整済み」と断言しない。プレイデータを外部送信しない。

テストは変更に対応するものを実行する。ローダーやゲーム環境がない場合は、ルールの検証と実機検証を分け、実行していない項目を明示する。

## 8. やってはいけないこと

- StS2とStS1のAPIを混同する、未確認のAPI名を事実として書く。
- 既存のユーザー変更を上書きする、セーブを検証用に無断削除する、ゲーム本体を直接書き換える。
- マップ階層を増やす、工房のために休憩所や商人を減らす、工房へのアクセスを検証せず完全ランダム配置で済ませる。
- ボスの希少保証を「通常・希少の混合3択から好きに選ぶ」に弱める。
- 報酬を提示するたびに再抽選する、同じ枠を再受領する、追加報酬を炉採取上限で消す。
- 炉の起動回数を再適用で回復させる、召喚・復活・複製・分解で素材を無限に得られるようにする。
- 戦闘内Exhaustを恒久デッキ削除として扱う。
- 素材・瓶の消費後に完成品を追加できない状態を放置する、満杯で希少素材を黙って捨てる。
- すべてを単純加算にする、または基本合成を消して完全固定レシピだけにする。
- 組み合わせごとに無制限の専用クラスを増やす、効果テキストからゲームルールを逆算する。
- 希少素材を自由に付け替える、回収する、同じカードへ複数刻む。
- レシピ発見をラン外の恒久ステータス強化にする。
- 通常カード報酬を勝手に廃止し、工房到達前の成長を失わせる。
- 初期段階でボス固有素材、特殊瓶、大量のカード、協力プレイ対応まで広げる。
- ビルドやテスト、実機プレイの成功を、未実施のまま報告する。

## 9. 作業記録と引き継ぎ

**セッションを始めたら、最初に`docs/session-log.md`の最新の項を読む。** 前回セッションでやったこと、未コミットの変更、ユーザーの返答待ちの事項が書いてある。セッションの終わり（またはユーザーが再起動しそうなとき）には、同ファイルの先頭に新しい項を追記する。

最初に対象版と現状を確認し、次に一つのマイルストーンに必要な最小変更を実装・検証する。未決事項のために無関係な作業まで止めない。

- `docs/session-log.md`：セッション間の引き継ぎログ（直近の作業、未コミットの変更、返答待ち事項）。
- `docs/research.md`：API・環境の調査結果、参照元、対象版、確認日。
- `docs/design-decisions.md`：採用した仮定、変更理由、未決事項、変更で影響するセーブ・テスト。
- `docs/test-results.md`：実行した検証、対象版、シード、結果、未実施項目。
- `README.md`：ユーザーがMODをビルド・導入・起動する手順。

各作業の報告には「何が動くようになったか」「何を検証したか」「残る制限・未決事項」を簡潔に記載する。ゲームループを遊べる状態にすることを優先し、仮値や未検証の互換性を確定事項に見せない。
